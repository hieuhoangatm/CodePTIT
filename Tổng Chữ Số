#include<stdio.h>
#include<math.h>
int main(){
	int n,a;
	scanf("%d", &a);
	while(a>0){
	   scanf("%d", &n);
	   a--;
	   int tongchuso=0;
	   while(n>0){
	        tongchuso+=(n%10);
	        n/=10;
	   }
	   printf("%d\n", tongchuso);
    }
}
// Hoàng Đình Hiếu
